<UserControl x:Class="ScoreBoardVtk.Wpf.Views.SettingsTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="720"
             d:DesignWidth="1280">
    <Grid DataContext="{Binding Workspace}" Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource CardBorderStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="140" />
                    <ColumnDefinition Width="220" />
                    <ColumnDefinition Width="150" />
                    <ColumnDefinition Width="150" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="COM port"
                           Style="{StaticResource FieldCaptionStyle}"
                           VerticalAlignment="Center" />

                <ComboBox Grid.Column="1"
                          Width="200"
                          Height="36"
                          ItemsSource="{Binding AvailablePorts}"
                          SelectedItem="{Binding SelectedPort}"
                          HorizontalAlignment="Left"
                          VerticalAlignment="Center" />

                <Button Grid.Column="2"
                        Content="Refresh Ports"
                        Width="130"
                        Command="{Binding RefreshPortsCommand}"
                        VerticalAlignment="Center"
                        Style="{StaticResource DeskButtonStyle}" />

                <Button Grid.Column="3"
                        Width="130"
                        Command="{Binding TogglePortCommand}"
                        VerticalAlignment="Center"
                        Style="{StaticResource PortToggleButtonStyle}" />
            </Grid>
        </Border>

        <Border Grid.Row="1" Margin="0,20,0,0" Style="{StaticResource CardBorderStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Grid Grid.Row="0" Grid.Column="0" Margin="0,0,16,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="Game mode" Style="{StaticResource FieldCaptionStyle}" />
                    <ComboBox Grid.Row="1"
                              Width="260"
                              Height="36"
                              HorizontalAlignment="Left"
                              ItemsSource="{Binding GameModes}"
                              DisplayMemberPath="Label"
                              SelectedItem="{Binding SelectedGameMode}" />
                </Grid>

                <Grid Grid.Row="0" Grid.Column="1" Margin="0,0,16,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="Font mode" Style="{StaticResource FieldCaptionStyle}" />
                    <ComboBox Grid.Row="1"
                              Width="260"
                              Height="36"
                              HorizontalAlignment="Left"
                              ItemsSource="{Binding FontModes}"
                              DisplayMemberPath="Label"
                              SelectedItem="{Binding SelectedFontMode}" />
                </Grid>

                <Grid Grid.Row="0" Grid.Column="2" Margin="0,0,16,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="Timer direction" Style="{StaticResource FieldCaptionStyle}" />
                    <ComboBox Grid.Row="1"
                              Width="260"
                              Height="36"
                              HorizontalAlignment="Left"
                              ItemsSource="{Binding TimerDirections}"
                              DisplayMemberPath="Label"
                              SelectedItem="{Binding SelectedTimerDirection}" />
                </Grid>

                <Grid Grid.Row="1" Grid.Column="0" Margin="0,16,16,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="Main timer preset" Style="{StaticResource FieldCaptionStyle}" />
                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="84" />
                            <ColumnDefinition Width="12" />
                            <ColumnDefinition Width="84" />
                            <ColumnDefinition Width="12" />
                            <ColumnDefinition Width="84" />
                        </Grid.ColumnDefinitions>
                        <ComboBox Grid.Column="0"
                                  Height="36"
                                  ItemsSource="{Binding PresetMinuteOptions}"
                                  SelectedItem="{Binding PresetMinutes}" />
                        <ComboBox Grid.Column="2"
                                  Height="36"
                                  ItemsSource="{Binding PresetSecondOptions}"
                                  SelectedItem="{Binding PresetSeconds}" />
                        <ComboBox Grid.Column="4"
                                  Height="36"
                                  ItemsSource="{Binding PresetTenthsOptions}"
                                  SelectedItem="{Binding PresetTenths}" />
                    </Grid>
                </Grid>

                <Grid Grid.Row="1" Grid.Column="1" Margin="0,16,16,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="Overtime preset" Style="{StaticResource FieldCaptionStyle}" />
                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="84" />
                            <ColumnDefinition Width="12" />
                            <ColumnDefinition Width="84" />
                            <ColumnDefinition Width="12" />
                            <ColumnDefinition Width="84" />
                        </Grid.ColumnDefinitions>
                        <ComboBox Grid.Column="0"
                                  Height="36"
                                  ItemsSource="{Binding PresetMinuteOptions}"
                                  SelectedItem="{Binding OvertimePresetMinutes}" />
                        <ComboBox Grid.Column="2"
                                  Height="36"
                                  ItemsSource="{Binding PresetSecondOptions}"
                                  SelectedItem="{Binding OvertimePresetSeconds}" />
                        <ComboBox Grid.Column="4"
                                  Height="36"
                                  ItemsSource="{Binding PresetTenthsOptions}"
                                  SelectedItem="{Binding OvertimePresetTenths}" />
                    </Grid>
                </Grid>

                <Grid Grid.Row="1" Grid.Column="2" Margin="0,16,16,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Row="0" Text="Main buzzer, sec" Style="{StaticResource FieldCaptionStyle}" />
                    <ComboBox Grid.Row="1"
                              Height="36"
                              HorizontalAlignment="Stretch"
                              ItemsSource="{Binding SignalDurationOptions}"
                              SelectedItem="{Binding MainSignalDurationSeconds}" />
                    <TextBlock Grid.Row="0" Grid.Column="2" Text="24 sec buzzer, tenths" Style="{StaticResource FieldCaptionStyle}" />
                    <ComboBox Grid.Row="1" Grid.Column="2"
            Height="36"
            HorizontalAlignment="Stretch"
            ItemsSource="{Binding ShotClockSignalOptions}"
            SelectedItem="{Binding ShotClockSignalDurationTenths}" />
                </Grid>

                
                <CheckBox Grid.Row="2"
                          Grid.Column="0"
                          Grid.ColumnSpan="3"
                          Content="Basketball fouls cap at 5"
                          IsChecked="{Binding CountFoulsToFive}"
                          FontSize="14"
                          FontWeight="SemiBold"
                          Margin="0,18,0,0"
                          VerticalAlignment="Center"
                          Style="{StaticResource SettingsBasketballOnlyCheckBoxStyle}" />

                <Button Grid.Row="0"
                        Grid.Column="3"
                        Content="Apply"
                        Width="130"
                        Margin="0,16,0,0"
                        HorizontalAlignment="Right"
                        Command="{Binding ApplySettingsCommand}"
                        Style="{StaticResource DeskButtonStyle}" />
                <Grid Grid.Row="3" Grid.ColumnSpan="5" Margin="0 16 0 0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <TextBlock Text="Keyboard Shortcuts"
                Style="{StaticResource PayloadHeaderTextStyle}"
                Margin="0,0,0,8" />

                    <TextBlock Grid.Row="1"
                Text="{Binding KeyboardShortcutCaptureText}"
                Style="{StaticResource FieldCaptionStyle}"
                Margin="0,0,0,12" />

                    <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding KeyboardShortcutBindings}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,0,0,10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="360" />
                                            <ColumnDefinition Width="160" />
                                            <ColumnDefinition Width="140" />
                                            <ColumnDefinition Width="120" />
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Grid.Column="0"
                                    Text="{Binding Label}"
                                    VerticalAlignment="Center"
                                    FontSize="14"
                                    FontWeight="SemiBold" />

                                        <TextBlock Grid.Column="1"
                                    Text="{Binding AssignedKeyDisplay}"
                                    VerticalAlignment="Center"
                                    FontFamily="Consolas"
                                    FontSize="14" />

                                        <Button Grid.Column="2"
                                 Content="{Binding AssignButtonText}"
                                 Command="{Binding BeginAssignCommand}"
                                 Width="120"
                                 Style="{StaticResource DeskButtonStyle}" />

                                        <Button Grid.Column="3"
                                 Content="Clear"
                                 Command="{Binding ClearCommand}"
                                 Width="100"
                                 Style="{StaticResource DeskButtonStyle}"
                                 Margin="12,0,0,0" />
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Grid>
        </Border>

        <!--<Border Grid.Row="2" Margin="0,20,0,0" Style="{StaticResource CardBorderStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock Text="Keyboard Shortcuts"
                           Style="{StaticResource PayloadHeaderTextStyle}"
                           Margin="0,0,0,8" />

                <TextBlock Grid.Row="1"
                           Text="{Binding KeyboardShortcutCaptureText}"
                           Style="{StaticResource FieldCaptionStyle}"
                           Margin="0,0,0,12" />

                <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding KeyboardShortcutBindings}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,0,0,10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="360" />
                                        <ColumnDefinition Width="160" />
                                        <ColumnDefinition Width="140" />
                                        <ColumnDefinition Width="120" />
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0"
                                               Text="{Binding Label}"
                                               VerticalAlignment="Center"
                                               FontSize="14"
                                               FontWeight="SemiBold" />

                                    <TextBlock Grid.Column="1"
                                               Text="{Binding AssignedKeyDisplay}"
                                               VerticalAlignment="Center"
                                               FontFamily="Consolas"
                                               FontSize="14" />

                                    <Button Grid.Column="2"
                                            Content="{Binding AssignButtonText}"
                                            Command="{Binding BeginAssignCommand}"
                                            Width="120"
                                            Style="{StaticResource DeskButtonStyle}" />

                                    <Button Grid.Column="3"
                                            Content="Clear"
                                            Command="{Binding ClearCommand}"
                                            Width="100"
                                            Style="{StaticResource DeskButtonStyle}"
                                            Margin="12,0,0,0" />
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>-->
    </Grid>
</UserControl>
