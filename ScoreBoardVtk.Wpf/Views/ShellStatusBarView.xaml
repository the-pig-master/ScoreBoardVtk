<UserControl x:Class="ScoreBoardVtk.Wpf.Views.ShellStatusBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="24"
             d:DesignWidth="1440">
    <StatusBar>
        <StatusBarItem Width="260">
            <TextBlock VerticalAlignment="Center">
                <TextBlock.Text>
                    <MultiBinding Converter="{StaticResource PortStatusTextConverter}">
                        <Binding Path="IsConnected" />
                        <Binding Path="ConnectedPortName" />
                    </MultiBinding>
                </TextBlock.Text>
            </TextBlock>
        </StatusBarItem>

        <StatusBarItem Width="220">
            <TextBlock Text="{Binding CurrentSnapshot.TimerPresetText, StringFormat=Preset: {0}}"
                       VerticalAlignment="Center" />
        </StatusBarItem>

        <StatusBarItem Width="760">
            <TextBlock Text="{Binding CurrentSnapshot.PayloadText, StringFormat=Payload: {0}}"
                       VerticalAlignment="Center"
                       TextTrimming="CharacterEllipsis" />
        </StatusBarItem>

        <StatusBarItem Width="320" HorizontalContentAlignment="Right">
            <TextBlock Text="{Binding SystemMessageText}"
                       VerticalAlignment="Center"
                       TextAlignment="Right"
                       TextTrimming="CharacterEllipsis" />
        </StatusBarItem>
    </StatusBar>
</UserControl>
